# Banner Block — Banner 区块摘录

Excerpt (first ~20 lines) and bilingual notes.

```typescript
import { Block } from 'payload'

const Banner: Block = {
  slug: 'banner',
  fields: [
    {
      name: 'title',
      type: 'text',
    },
```

EN: Banner block provides a hero-like block with title, media and call-to-action.
CN: Banner 区块提供标题、媒体和 CTA 的英雄式区块。
```